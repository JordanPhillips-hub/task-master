import styled from "styled-components";

const Tag = styled.div`
  background-color: var(--clr-light-blue-transparent);
  font-size: 0.75rem;
  font-weight: 400;
  padding: 0.375em 0.5em;
  border-radius: 60px;
`

export default Tag;